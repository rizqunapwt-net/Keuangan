Ringkasan singkat:

- Apa yang dibuat: `docs/API_CONTRACT.md` ditambahkan, berisi kontrak API resmi untuk fase D.
- Endpoint yang terdokumentasi: `POST /auth/login`, `POST /attendance/check-in`, `POST /attendance/check-out`, `PUT /attendance/{id}/correct`.
- Status fase: FASE D – selesai untuk dokumentasi kontrak (siap untuk handover/frontend preparation).

Catatan:

- Tidak ada perubahan kode backend atau frontend.
- Tidak ada endpoint baru ditambahkan pada runtime; dokumen hanya berfungsi sebagai pegangan frontend.

Status: selesai.
